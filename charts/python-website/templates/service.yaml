apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-website.fullname" . }}
  labels:
    {{- include "python-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-website.selectorLabels" . | nindent 4 }}
